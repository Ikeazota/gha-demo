name: Github Action Demo
on: push
jobs:
  git-actions1:
    runs-on: ubuntu-latest
    steps:
      - name: checkout the code
        uses: actions/checkout@v4
        
      - name: Print index file
        run: cat index.html
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test phase
        run: echo "running test cases"

